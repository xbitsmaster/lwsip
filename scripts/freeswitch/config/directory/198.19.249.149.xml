<?xml version="1.0" encoding="utf-8"?>
<include>
  <domain name="198.19.249.149">
    <params>
      <param name="dial-string" value="{^^:sip_invite_domain=${dialed_domain}:presence_id=${dialed_user}@${dialed_domain}}${sofia_contact(*/${dialed_user}@${dialed_domain})}"/>
      <!-- These are required for Verto to function properly -->
      <param name="jsonrpc-allowed-methods" value="verto"/>
      <!-- <param name="jsonrpc-allowed-event-channels" value="demo,conference,presence"/> -->
    </params>

    <variables>
      <variable name="record_stereo" value="true"/>
      <variable name="default_gateway" value="$${default_provider}"/>
      <variable name="default_areacode" value="$${default_areacode}"/>
      <variable name="transfer_fallback_extension" value="operator"/>
    </variables>

    <groups>
      <group name="default">
        <users>
          <!-- Include all XML user files in this directory -->
          <X-PRE-PROCESS cmd="include" data="1000.xml"/>
          <X-PRE-PROCESS cmd="include" data="1001.xml"/>
          <X-PRE-PROCESS cmd="include" data="1002.xml"/>
          <X-PRE-PROCESS cmd="include" data="1003.xml"/>
        </users>
      </group>
    </groups>

  </domain>
</include>
